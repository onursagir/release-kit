{
  "name": "@re-kit/cli",
  "version": "0.1.3",
  "description": "CLI for release-kit: status, plan, version, add.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/onursagir/release-kit.git",
    "directory": "packages/cli"
  },
  "type": "module",
  "bin": {
    "re-kit": "./dist/bin.js"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -b && chmod +x dist/bin.js"
  },
  "dependencies": {
    "@re-kit/core": "^0.2.0",
    "@re-kit/platform-github": "^0.1.0",
    "jiti": "^2.4.0"
  }
}
