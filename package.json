{
  "name": "release-kit",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.10.0"
  },
  "workspaces": ["packages/*"],
  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "typecheck": "tsc -b packages/core packages/strategy-npm packages/strategy-rekit packages/strategy-git-tag packages/platform-github packages/cli",
    "build": "tsc -b packages/core packages/strategy-npm packages/strategy-rekit packages/strategy-git-tag packages/platform-github packages/cli",
    "test": "vitest run"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.0"
  }
}
